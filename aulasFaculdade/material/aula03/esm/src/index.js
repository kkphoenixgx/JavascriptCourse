/**
 * Inverte o nome informado e o retorna.
 *
 * @param {string} nome
 * @returns {string}
 */
export function inverterNome( nome ) {
    return nome.split( '' ).reverse().join( '' );
}

const UNIVERSIDADE = 'CEFET';

export default UNIVERSIDADE;